import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.standard.wallpaper',
  appName: 'Standard Wallpaper',
  webDir: 'dist'
};

export default config;
